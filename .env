# Vertica MCP default development configuration
# Replace these placeholders with the public Vertica endpoint you want the MCP
# service to connect to before deploying. The defaults intentionally point to a
# local-only address so the service never attempts to reach an unexpected
# database host out of the box.
DB_HOST=127.0.0.1
DB_PORT=5433
DB_USER=mcp_app
DB_PASSWORD=change-me-please
DB_NAME=vertica
MAX_ROWS=1000
QUERY_TIMEOUT_S=15
POOL_SIZE=8
ALLOWED_SCHEMAS=public
PUBLIC_HTTP_PORT=8000
