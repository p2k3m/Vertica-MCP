# Sample configuration for a publicly reachable Vertica cluster with TLS enabled.
# Copy this file to .env (or merge the settings into your own environment file)
# before starting the MCP service. Replace the placeholder values with your
# organisation's production credentials.
DB_HOST=vertica-us-east-1.example.com
DB_PORT=5433
DB_USER=app_user
DB_PASSWORD=super-secret-password
DB_NAME=analytics
# Enforce TLS with full certificate validation.
DB_TLSMODE=verify-full
# Provide the CA bundle that signs the public Vertica certificate.
DB_TLS_CAFILE=/etc/ssl/certs/ca-bundle.crt
# Allow the MCP to read from both the public and reporting schemas.
ALLOWED_SCHEMAS=public,reporting
# Optional: increase timeouts for long-running remote queries.
QUERY_TIMEOUT_S=30
