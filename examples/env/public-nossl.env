# Sample configuration for connecting to a legacy Vertica deployment where TLS
# is disabled. Copy these settings into your environment file only when you
# control the network path to the database and understand the risks of
# transmitting credentials without encryption.
DB_HOST=legacy-vertica.example.net
DB_PORT=5433
DB_USER=reporting_user
DB_PASSWORD=insecure-demo-password
DB_NAME=warehouse
# Explicitly disable TLS so the driver falls back to plain TCP.
DB_TLSMODE=disable
# Increase connection retries to handle the longer WAN round-trip time.
DB_CONNECTION_RETRIES=5
DB_CONNECTION_RETRY_BACKOFF_S=1.5
